/**
 * This file was generated by
 * ╭─────────────────────────╮
 * │░█▀▀░█▀▀░█▀▄░█▀▄░▀█▀░█▀▀░│
 * │░█▀▀░█▀▀░█▀▄░█▀▄░░█░░█░░░│
 * │░▀░░░▀▀▀░▀░▀░▀░▀░▀▀▀░▀▀▀░│
 * ╰─────────────────────────╯
 * Powered by napi.rs
 */
/**
 * 64-bit values cross the boundary as decimal strings, so the binding stays on
 * the same N-API level as the rest of the module and needs no BigInt support
 * from the host. The TypeScript layer converts to and from `bigint`.
 */
export type BigIntString = string;
/**
 * SipHash-2-4 under a 16-byte key, as Bitcoin and Dash use it (BIP 158 filter
 * matching, BIP 152 short ids). The digest is the little-endian bytes of the
 * 64-bit result, the order those protocols serialize it in.
 */
export declare function siphash24(key: Uint8Array, data: Uint8Array): Uint8Array;
/** Size of a SipHash key: two 64-bit halves, little-endian. */
export declare const SIPHASH24_KEY_LENGTH: number;
/** Size of the SipHash-2-4 digest. */
export declare const SIPHASH24_OUTPUT_LENGTH: number;
/** Same as [`siphash24`], with the key, the data and the digest as hex strings. */
export declare function siphash24Hex(key: string, data: string): string;
/**
 * Same as [`siphash24`], keyed by the two 64-bit halves and returning the
 * 64-bit result — the shape BIP 158 filter matching works in.
 */
export declare function siphash24WithKeys(k0: BigIntString, k1: BigIntString, data: Uint8Array): BigIntString;
/** Size of the input the X11 chain consumes: a Dash block header. */
export declare const X11_INPUT_LENGTH: number;
/** Size of the X11 digest. */
export declare const X11_OUTPUT_LENGTH: number;
/**
 * Hashes an 80-byte block header with the X11 chain (Blake, BMW, Groestl,
 * Skein, JH, Keccak, Luffa, CubeHash, SHAvite-3, SIMD, ECHO).
 */
export declare function x11Hash(input: Uint8Array): Uint8Array;
/** Same as [`x11_hash`], with the header and the digest as hex strings. */
export declare function x11HashHex(input: string): string;
